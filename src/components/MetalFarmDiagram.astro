---
// ASCII art de l'architecture Metal Farm
---

<div class="bg-slate-950 border border-slate-800 p-6 rounded-lg overflow-x-auto">
  <pre class="font-mono text-xs text-emerald-400/80 leading-relaxed">
{`
┌─────────────────────────────────────────────────────────────────┐
│                    THE METAL FARM CLUSTER                       │
│                  Bare Metal Kubernetes Architecture              │
└─────────────────────────────────────────────────────────────────┘

        ┌──────────────┐
        │  PXE Server  │ ───────── Provisioning Automation
        │   + MaaS     │
        └──────┬───────┘
               │
    ┌──────────┴──────────┐
    │                     │
┌───▼────┐  ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐
│ NODE-1 │  │ NODE-2 │  │ NODE-3 │  │ NODE-4 │  │ NODE-5 │
│ Master │  │ Worker │  │ Worker │  │ Worker │  │ Worker │
│  etcd  │  │  Pod   │  │  Pod   │  │  Pod   │  │  Pod   │
└────────┘  └────────┘  └────────┘  └────────┘  └────────┘
    │           │           │           │           │
    └───────────┴───────────┴───────────┴───────────┘
                        │
                   ┌────▼────┐
                   │  CNI    │ ────── Calico/Cilium (eBPF)
                   │ Network │
                   └─────────┘
                        │
                   ┌────▼────┐
                   │ Storage │ ────── Longhorn / Ceph
                   │  Layer  │
                   └─────────┘
                        │
                   ┌────▼────┐
                   │  Obs.   │ ────── Prometheus + Grafana
                   │ Stack   │
                   └─────────┘

┌──────────────────────────────────────────────────────────────┐
│ Cattle Philosophy: Kill & Rebuild in < 3min                 │
│ Zero Hypervisor • Full Control • Latency < 2ms              │
└──────────────────────────────────────────────────────────────┘
`}
  </pre>
</div>
